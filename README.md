# Parkinson-disease-detection
This repository focuses on detecting Parkinson’s disease using machine learning models. It includes data preprocessing, feature engineering, model training, and evaluation with metrics such as accuracy and ROC-AUC. The project uses biomedical voice datasets to build reliable prediction models.
